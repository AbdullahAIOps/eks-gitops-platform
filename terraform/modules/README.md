# Internal Terraform modules

Reserved for thin, opinionated wrappers specific to this platform (for example a
`platform-namespace` module that bundles a namespace + ResourceQuota + default
NetworkPolicy). Intentionally empty in this reference repo — see the design
notes in `../README.md`.
